# Backend API URL
# Leave as /api for production (Caddy proxies /api/* to backend via BACKEND_URL)
# For local dev this is handled automatically by Vite proxy (no need to set)
VITE_API_URL=/api

# Set this in your Railway/hosting environment — NOT in this file
# BACKEND_URL=https://your-backend-service.railway.app
