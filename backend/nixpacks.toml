[phases.setup]
nixPkgs = ["python312", "python312Packages.pip", "git", "curl"]

[phases.install]
cmds = ["python3 -m pip install -r requirements.txt"]

[phases.build]
cmds = ["python3 -m pip install semgrep bandit || true"]

[start]
cmd = "uvicorn main:app --host 0.0.0.0 --port $PORT"
